"use strict";
var Base = require('../../base')
var util = require('../../util')

//----injectable part of module-----
exports.$flags = {
  $defer: function( val ) {
    //lets do some defer! (defer is also a base)

  }
}
