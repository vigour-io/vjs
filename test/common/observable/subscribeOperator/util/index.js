// var shared = require('../../../../../lib/observable/subscribe/shared')
// var code = shared.code
// var id = shared.id
// var horizontal = shared.horizontal
// var vertical = shared.vertical

// describe('bitshift', function () {
//   it('decodes to original values', function () {
//     for (var a = 255; a >= 0; a--) {
//       var b = a
//       var uid = (a + 1) * 256 - 1
//       var int = code(uid, a, b)

//       expect(
//         horizontal(int) === a &&
//         vertical(int) === b &&
//         id(int) === uid
//       ).ok
//     }
//   })
// })
