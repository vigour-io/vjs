describe('defer', function() {

  var Observable = require('../../../../../lib/base')


  it('should create a new event and fire with a timeout', function() {
    var a = new Observable({
      $key:'a' //this will just set $key

    })


  })

})
