'use strict'
// data store it mem efficient -- maybe take this with it?

// firing too many updates (always going in update instances for each emitter is not so nice!)

// do removals
// fix for instances
// handle property listener for fields

// this is the instances case
describe('overwrites', function () {
  require('./input')
  require('./property')
})
