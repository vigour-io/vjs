describe('methods', function () {
  require('./convert/')
  require('./plain/')
  require('./flatten/')
  require('./each/')
  require('./filter/')
  require('./find/')
  require('./get/')
  require('./keys/')
  require('./lookdown/')
  require('./lookup/')
  require('./map/')
  require('./setwithpath/')
  require('./tostring/')
})
