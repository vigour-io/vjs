describe('methods', function () {
  require('./plain/')
  require('./flatten/')
  require('./serialize/')
  require('./each/')
  require('./filter/')
  require('./find/')
  require('./get/')
  require('./keys/')
  require('./lookdown/')
  require('./lookup/')
  require('./map/')
  require('./setwithpath/')
  require('./tostring/')
})
